<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="cr_register_payment_wizard_view_form" model="ir.ui.view">
        <field name="name">cr.register.payment.wizard.view.form</field>
        <field name="model">cr.register.payment.wizard</field>
        <field name="arch" type="xml">
            <form>
                <field name="sale_order_id" invisible="1" />
                <field name="company_id" invisible="1" force_save="1" />
                <field name="available_journal_ids" invisible="1" />
                <field name="available_partner_bank_ids" invisible="1" />
                <field name="available_payment_method_line_ids" invisible="1" />
                <field name="company_currency_id" invisible="1" />
                <field name="payment_type" invisible="1" />
                <field name="show_partner_bank_account" invisible="1" />
                <field name="require_partner_bank_account" invisible="1" />
                
                <group>
                    <group name="group1">
                        <field name="journal_id" options="{'no_open': True, 'no_create': True}"
                            required="1" />
                        <field name="payment_method_line_id" required="1"
                            options="{'no_create': True, 'no_open': True}" />
                        <field name="partner_bank_id"
                            attrs="{'invisible': [('show_partner_bank_account', '=', False)],'readonly':[('payment_type', '==', 'inbound')],'required':[('require_partner_bank_account','=',True)]}"
                            context="{'display_account_trust': True}" />
                    </group>
                    <group name="group2">
                        <label for="amount" />
                        <div name="amount_div" class="o_row">
                            <field name="amount" />
                            <field name="currency_id" required="1"
                                options="{'no_create': True, 'no_open': True}"
                                groups="base.group_multi_currency" />
                        </div>
                        <field name="payment_date" />
                    </group>
                </group>
                <footer>
                    <button string="Create Payment" name="action_create_payments" type="object"
                        class="oe_highlight" data-hotkey="q" />
                    <button string="Discard" class="btn btn-secondary" special="cancel"
                        data-hotkey="x" />
                </footer>
            </form>
        </field>
    </record>

</odoo>