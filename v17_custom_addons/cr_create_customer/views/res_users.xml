<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_cr_res_users_form_inherit" model="ir.ui.view">
        <field name="name">cr.res.users.form.inherit</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">

            <xpath expr="//notebook" position="inside">
                <page string="Allowed Products" name="allowed_products">
                   <group>
                       <field name="cr_product_ids" widget="many2many_tags"/>
                       <field name="cr_product_category_ids" widget="many2many_tags"/>
                   </group>
                </page>
            </xpath>

        </field>
    </record>
</odoo>