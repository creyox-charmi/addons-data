<odoo>
    <record id="view_product_template_form_inherit_price_groupby" model="ir.ui.view">
        <field name="name">product.template.form.inherit.price.groupby</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='sales_price']" position="replace">
                <page
                        string="Prices"
                        name="sales_price"
                        groups="product.group_product_pricelist"
                        invisible="not sale_ok"
                >
                    <field
                            name="pricelist_rule_ids"
                            widget="one2many_grouped"
                            context="{
    'form_view_ref': 'product.product_pricelist_item_product_template_form_view',
    'default_product_tmpl_id': id,
    'default_group_by': 'pricelist_id'
}"
                            options="{'groupBy': 'pricelist_id'}"
                    >
                        <list createGroup="true" default_group_by="pricelist_id">
                            <control>
                                <create name="add_product_price" string="Add a price"/>
                            </control>
                            <field name="pricelist_id" required="is_pricelist_required"/>
                            <field name="name" string="Applied On"
                                   column_invisible="parent.product_variant_count &lt; 2"/>
                            <field name="price"/>
                            <field name="min_quantity"/>
                            <field name="date_start" optional="hide"/>
                            <field name="date_end" optional="hide"/>
                            <field name="company_id" groups="base.group_multi_company" optional="hide"/>
                            <field name="product_tmpl_id" column_invisible="True"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>